import { IdleOnScrollDirective } from './idle-on-scroll.directive';

describe('IdleOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new IdleOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
