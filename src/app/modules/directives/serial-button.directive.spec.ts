import { SerialButtonDirective } from './serial-button.directive';

describe('SerialButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SerialButtonDirective();
    expect(directive).toBeTruthy();
  });
});
